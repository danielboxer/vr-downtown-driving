[project]
name = "Sumo2UnityTool"
version = "0.1.0"
description = "Sumo2Unity tool for running the SUMO simulation and communicating to Unity"
readme = "README.md"
requires-python = ">=3.12"

dependencies = ["pyzmq>=27.1.0"]

[dependency-groups]
dev = ["pyinstaller>=6.19.0", "ruff>=0.15.5"]
